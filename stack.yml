version: '3.9'

services:
  traefik:
    image: traefik:2.10
    command:
      - "--providers.docker.swarmMode=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.http.address=:80"
      - "--entrypoints.https.address=:443"
      - "--certificatesresolvers.http.acme.httpchallenge=true"
      - "--certificatesresolvers.http.acme.httpchallenge.entrypoint=http"
      - "--certificatesresolvers.http.acme.email=tu-correo@gmail.com"
      - "--certificatesresolvers.http.acme.storage=/acme.json"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - "acme:/acme.json"
    deploy:
      labels:
        - "traefik.enable=true"
    networks:
      - traefik-public

  app:
    image: ghcr.io/tefy89/traefik-estefany:1.0.0
    deploy:
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.app.rule=Host(`traefik-estefany.byronrm.com`)"
        - "traefik.http.routers.app.entrypoints=http"
        - "traefik.http.routers.app-secure.rule=Host(`traefik-estefany.byronrm.com`)"
        - "traefik.http.routers.app-secure.entrypoints=https"
        - "traefik.http.routers.app-secure.tls=true"
        - "traefik.http.routers.app-secure.tls.certresolver=http"
        - "traefik.http.services.app.loadbalancer.server.port=5000"
        - "traefik.docker.network=traefik-public"
    networks:
      - traefik-public

volumes:
  acme:

networks:
  traefik-public:
    external: true
